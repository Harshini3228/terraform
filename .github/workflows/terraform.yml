name: Terraform Deployment

on:
  push:
    branches:
      - main
permissions:
      id-token: write
      contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 0.15.0    

      # - name: Azure Login
      #   run: az login --service-principal -u 9f853451-c0f9-4f88-be1d-2e578bbb76be -p 3zf8Q~R9q4uPA9t2p.u2bxUNr5ygVC5oVcpnacoo --tenant 4284bc2c-7bea-4e03-b48a-128cd2886ddb
        # uses: azure/actions/login@v1
        # with:
        #   creds: ${{ secrets.AZURE_CREDENTIALS }}
      # - name: Azure Login
      #   uses: azure/login@v1
      #   with:
      #    client-id: ${{ secrets.AZURE_CLIENT_ID }}
      #    tenant-id: ${{ secrets.AZURE_TENANT_ID }}
      #    allow-no-subscriptions: true
      #    enable-AzPSSession: true

      # - name: Azure CLI script
      #   uses: azure/CLI@v1
      #   with:
      #     azcliversion: latest
      #     inlineScript: |
      #       az account show

      - name: Terraform Init
        run: terraform init

      - name: Terraform Plan
        run: terraform plan

      # - name: Terraform Apply
      #   run: terraform apply -auto-approve
